# public/hdri/

Optionale HDRI-Environment-Map (`.hdr`) für den Mesh-Track des Twins.

```js
environment: { hdri: '/hdri/studio.hdr' },
```

Ohne HDRI nutzt der Mesh-Track ein drei-Preset. Der Splat-Track braucht keine
Environment-Map.
