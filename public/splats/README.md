# public/splats/

Gaussian-Splat-Dateien für den eingebetteten Twin.

Spark.js unterstützt: `.ply` (auch komprimiert), `.spz`, `.splat`, `.ksplat`,
`.sog`.

In `scene.config.js` referenzieren mit einem Pfad ab public-Root, z.B.
`/splats/showroom.spz`. Die Demo nutzt eine öffentliche URL.
